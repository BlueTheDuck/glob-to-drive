{
  "name": "drive",
  "version": "0.0.1",
  "description": "Uploads files to Drive",
  "main": "src/index.js",
  "author": "BlueTheDuck",
  "license": "GPL-3.0-only",
  "scripts": {
    "compile": "ncc build src/index.js -o dist -m"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "await-semaphore": "^0.1.3",
    "glob": "^7.1.6",
    "googleapis": "^61.0.0"
  },
  "devDependencies": {
    "@google-cloud/local-auth": "^1.0.0",
    "@vercel/ncc": "^0.28.6",
    "dotenv": "^8.2.0"
  }
}
